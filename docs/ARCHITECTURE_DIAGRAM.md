# SmartCart - Azure + Firebase Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         SMARTCART APP                            │
└───────────────┬─────────────────────────────────────────────────┘
                │
                │
        ┌───────▼────────┐
        │   USER LOGIN   │
        │  ┌──────────┐  │
        │  │ Firebase │  │ ← Email/Password
        │  │   Auth   │  │ ← Google Sign-In
        │  └──────────┘  │
        └───────┬────────┘
                │
                │ (Get User UID)
                │
    ┌───────────▼────────────┐
    │   DATA LAYER (Dual)    │
    ├────────────┬───────────┤
    │            │           │
┌───▼───┐    ┌──▼────┐      │
│ HIVE  │    │ AZURE │      │
│(Local)│◄──►│TABLES │      │
└───────┘    └───────┘      │
 Offline      Cloud Sync    │
 Storage      Multi-Device  │
└────────────────────────────┘
```

## Data Tables Structure

```
AZURE TABLES (Cloud Storage)
│
├─ Users Table
│  PartitionKey: 'user'
│  RowKey: Firebase UID
│  └─ Email, DisplayName, Provider, LastLogin
│
├─ Products Table
│  PartitionKey: User ID ← Isolates data per user
│  RowKey: Product ID
│  └─ Name, Barcode, Category, Quantity, ExpiryDate...
│
├─ Nutrition Table
│  PartitionKey: User ID
│  RowKey: Date (YYYY-MM-DD)
│  └─ Calories, Protein, Carbs, Fat, Products[]
│
├─ ShoppingList Table
│  PartitionKey: User ID
│  RowKey: Item ID
│  └─ ProductName, Quantity, IsPurchased
│
└─ UserSettings Table
   PartitionKey: User ID
   RowKey: 'settings'
   └─ CalorieGoal, ProteinGoal, CarbGoal, FatGoal
```

## Authentication Flow

```
┌─────────────┐
│ User Opens  │
│     App     │
└──────┬──────┘
       │
       ▼
┌──────────────────┐      ┌──────────────────┐
│ Already Logged   │ NO   │  Show Login/     │
│      In?         │─────►│  Signup Screen   │
└────────┬─────────┘      └────────┬─────────┘
         │YES                      │
         │                         ▼
         │              ┌──────────────────────┐
         │              │ User Chooses:        │
         │              │ 1. Email/Password    │
         │              │ 2. Google Sign-In    │
         │              └──────────┬───────────┘
         │                         │
         │                         ▼
         │              ┌──────────────────────┐
         │              │  Firebase Auth       │
         │              │  Authenticates User  │
         │              └──────────┬───────────┘
         │                         │
         │                         │ (Get UID)
         │                         │
         │              ┌──────────▼───────────┐
         │              │ Store/Update User    │
         │              │ Profile in Azure     │
         │              │ Users Table          │
         │              └──────────┬───────────┘
         │                         │
         └─────────────────────────┘
                       │
                       ▼
         ┌──────────────────────────┐
         │ Sync User's Data from    │
         │ Azure to Local (Hive)    │
         └──────────┬───────────────┘
                    │
                    ▼
         ┌──────────────────────────┐
         │ App Ready with User Data │
         └──────────────────────────┘
```

## Data Sync Flow

```
USER ADDS PRODUCT
       │
       ▼
┌──────────────────┐
│ Save to Hive     │ ← Instant (Offline Works!)
│ (Local Storage)  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Check Internet?  │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
   YES       NO
    │         │
    ▼         └──► Queue for later sync
┌──────────────────┐
│ Sync to Azure    │
│ Products Table   │
│ PartitionKey:    │
│   User ID        │
│ RowKey:          │
│   Product ID     │
└──────────────────┘
```

## Multi-Device Sync

```
Device A                    Azure Tables                Device B
┌────────┐                 ┌──────────┐               ┌────────┐
│        │                 │          │               │        │
│  Add   │───────────────►│  Store   │◄──────────────│  Sync  │
│Product │  Sync to Cloud │ Product  │  Load from    │  Data  │
│        │                 │          │     Cloud     │        │
└────────┘                 └──────────┘               └────────┘
    ↓                           │                          ↓
Local Hive                  Partition by              Local Hive
has product               User ID ensures            gets product
instantly                   data isolation            on login/refresh
```

## Cost Comparison (1000 Users)

```
┌─────────────────────────────────────────────────────┐
│              FIRESTORE ONLY (OLD)                   │
├─────────────────────────────────────────────────────┤
│ Firebase Auth:     FREE                             │
│ Firestore:         $36.50/month                     │
│ ─────────────────────────────────────────────────   │
│ TOTAL:             $36.50/month  ❌                 │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│         AZURE + FIREBASE (NEW) ✅                   │
├─────────────────────────────────────────────────────┤
│ Firebase Auth:     FREE                             │
│ Azure Tables:      $1.10/month                      │
│ ─────────────────────────────────────────────────   │
│ TOTAL:             $1.10/month  ✅                  │
│                                                      │
│ SAVINGS:           $35.40/month (97% cheaper!)      │
└─────────────────────────────────────────────────────┘
```

## Security Model

```
┌─────────────────────────────────────────────────────┐
│                  SECURITY LAYERS                     │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Layer 1: Firebase Authentication                   │
│  └─ Secure login (Email/Password or Google)         │
│  └─ Generates unique UID per user                   │
│                                                      │
│  Layer 2: Azure Authentication                      │
│  └─ Shared Key signature (HMAC-SHA256)              │
│  └─ HTTPS encryption in transit                     │
│                                                      │
│  Layer 3: Data Isolation                            │
│  └─ PartitionKey = User ID                          │
│  └─ Users can ONLY access their own data            │
│  └─ No cross-user queries possible                  │
│                                                      │
│  Layer 4: Local Encryption (Optional)               │
│  └─ Hive data encrypted on device                   │
│  └─ Biometric authentication support                │
│                                                      │
└─────────────────────────────────────────────────────┘
```

## File Structure

```
lib/
├── services/
│   ├── azure_table_service.dart     ← NEW! Azure operations
│   ├── auth_service.dart             ← UPDATED! Azure sync
│   ├── barcode_service.dart          ← Unchanged
│   └── firestore_service.dart        ← Can be removed (deprecated)
│
├── providers/
│   ├── inventory_provider.dart       ← UPDATED! Uses Azure
│   └── nutrition_provider.dart       ← UPDATED! Uses Azure
│
└── models/
    ├── product.dart                  ← Unchanged
    └── nutrition.dart                ← Unchanged

docs/
├── ARCHITECTURE.md                   ← NEW! Complete overview
├── AZURE_SETUP.md                    ← NEW! Setup guide
└── IMPLEMENTATION_SUMMARY.md         ← NEW! Quick start
```

## Quick Start Checklist

```
□ Step 1: Create Azure Storage Account
□ Step 2: Create 5 tables (Users, Products, Nutrition, ShoppingList, UserSettings)
□ Step 3: Copy account name and access key
□ Step 4: Update lib/services/azure_table_service.dart with credentials
□ Step 5: Run flutter pub get
□ Step 6: Run flutter run
□ Step 7: Test signup/login
□ Step 8: Test adding products
□ Step 9: Test offline mode
□ Step 10: Deploy! 🚀
```
